# Computer Science Facts — [DEMO, fabricated dates]

_Every entry below was appended by a backdated commit for a demonstration._

- **2025-06-21:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-06-21:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-06-22:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-06-22:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-06-22:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-06-22:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-06-23:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-06-24:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-06-24:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-06-24:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-06-24:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-06-24:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-06-24:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-06-25:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-06-25:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-06-25:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-06-25:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-06-25:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-06-26:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-06-26:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-06-26:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-06-26:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-06-26:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-06-26:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-06-26:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-06-27:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-06-27:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-06-27:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-06-27:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-06-27:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-06-27:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-06-28:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-06-28:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-06-28:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-06-28:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-06-29:** The term 'bug' predates computers; Grace Hopper famously taped a real moth into a 1947 logbook.
- **2025-06-29:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-06-29:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-06-29:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-06-29:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-06-29:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-06-30:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-06-30:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-06-30:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-06-30:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-06-30:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-01:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-02:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-02:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-02:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-02:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-07-02:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-02:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-03:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-04:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-04:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-04:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-04:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-04:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-04:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-04:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-05:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-05:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-05:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-07-05:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-06:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-07-06:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-06:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-06:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-06:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-06:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-06:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-07-07:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-07:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-07:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-08:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-08:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-09:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-09:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-09:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-09:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-09:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-09:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-07-09:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-07-10:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-10:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-10:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-10:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-10:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-10:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-11:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-11:** The term 'bug' predates computers; Grace Hopper famously taped a real moth into a 1947 logbook.
- **2025-07-11:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-12:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-12:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-12:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-12:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-12:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-13:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-13:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-13:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-07-13:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-14:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-14:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-14:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-07-14:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-07-14:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-14:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-14:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-07-15:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-16:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-16:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-16:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-16:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-16:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-07-16:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-17:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-17:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-17:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-18:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-18:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-18:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-18:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-18:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-18:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-18:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-19:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-19:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-19:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-20:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-20:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-21:** The term 'bug' predates computers; Grace Hopper famously taped a real moth into a 1947 logbook.
- **2025-07-21:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-21:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-22:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-22:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-22:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-23:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-23:** The halting problem is undecidable: no general algorithm can tell if any program will stop.
- **2025-07-23:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-23:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-23:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-24:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-24:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-24:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-24:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-24:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-24:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-07-25:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-25:** A balanced binary tree keeps height ~log n, which is what keeps its operations fast.
- **2025-07-25:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-25:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-25:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-25:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-25:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-26:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-26:** The term 'bug' predates computers; Grace Hopper famously taped a real moth into a 1947 logbook.
- **2025-07-26:** A SHA-256 hash is 256 bits — 64 hexadecimal characters — regardless of input size.
- **2025-07-27:** Quicksort averages O(n log n) but degrades to O(n^2) on already-sorted input with naive pivots.
- **2025-07-27:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-27:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-27:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-27:** Deadlock needs four conditions at once: mutual exclusion, hold-and-wait, no preemption, circular wait.
- **2025-07-28:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-28:** Caches exploit locality of reference — recently/nearby-accessed data is likely to be used again.
- **2025-07-28:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-28:** Git stores snapshots, not diffs; identical file contents are stored only once via content hashing.
- **2025-07-28:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-28:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
- **2025-07-29:** Floating-point can't represent 0.1 exactly, which is why 0.1 + 0.2 != 0.3 in most languages.
- **2025-07-29:** The term 'bug' predates computers; Grace Hopper famously taped a real moth into a 1947 logbook.
- **2025-07-29:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-30:** A hash table gives average O(1) lookup, but worst-case O(n) when every key collides.
- **2025-07-30:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-07-30:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-30:** ASCII uses 7 bits (128 values); UTF-8 extends this to all of Unicode while staying ASCII-compatible.
- **2025-07-30:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-31:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-07-31:** Binary search runs in O(log n) time but requires the input to be sorted first.
- **2025-07-31:** There are exactly 1024 bytes in a kibibyte (KiB), but 1000 in a kilobyte (kB).
- **2025-07-31:** TCP guarantees ordered, reliable delivery; UDP trades those guarantees for lower latency.
- **2025-07-31:** Big-O describes growth, not speed: an O(n) algorithm can beat an O(log n) one for small n.
